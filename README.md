Restfull API is shown and Swagger is used for tests. MsSql is used for the database. The project is written with classical architecture. Json web token is used in roles and authorization transactions.
